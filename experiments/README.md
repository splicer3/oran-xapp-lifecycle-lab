# Experiments

This directory contains analysis material that is not directly invoked by Ansible.

Runnable experiment automation stays under `ansible/` because those playbooks depend on local roles, templates, inventory examples, and scripts.
