# ------------------------
# Default: normalize line endings
# ------------------------
* text=auto eol=lf

# ------------------------
# YAML / Ansible
# ------------------------
*.yml  text eol=lf diff=yaml
*.yaml text eol=lf diff=yaml

# GitHub Linguist: classify Ansible-owned YAML as Ansible without
# reclassifying GitHub Actions or Kubernetes/Istio Jinja templates.
ansible/**/*.yml  linguist-language=Ansible
ansible/**/*.yaml linguist-language=Ansible

# ------------------------
# Markdown / Docs
# ------------------------
*.md   text eol=lf diff=markdown
*.txt  text eol=lf

# ------------------------
# Templates
# ------------------------
*.j2   text eol=lf
*.tpl  text eol=lf

# ------------------------
# Code
# ------------------------
*.py   text eol=lf diff=python
*.sh   text eol=lf

# ------------------------
# Configs
# ------------------------
*.ini  text eol=lf
*.cfg  text eol=lf
*.conf text eol=lf

# ------------------------
# Binary / Skip diff
# ------------------------
*.box  binary
*.crt  binary
*.key  binary
*.pem  binary
