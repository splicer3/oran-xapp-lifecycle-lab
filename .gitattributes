# ------------------------
# Default: normalize line endings
# ------------------------
* text=auto eol=lf

# ------------------------
# YAML / Ansible
# ------------------------
*.yml  text eol=lf diff=yaml
*.yaml text eol=lf diff=yaml

# GitHub Linguist: include Ansible-owned YAML in language stats without
# reclassifying GitHub Actions or Kubernetes/Istio Jinja templates.
# Linguist does not define a separate Ansible language, so these show as YAML.
ansible/**/*.yml  linguist-language=YAML linguist-detectable
ansible/**/*.yaml linguist-language=YAML linguist-detectable

# ------------------------
# Markdown / Docs
# ------------------------
*.md   text eol=lf diff=markdown
*.txt  text eol=lf

# ------------------------
# Templates
# ------------------------
*.j2   text eol=lf
*.tpl  text eol=lf

# ------------------------
# Code
# ------------------------
*.py   text eol=lf diff=python
*.sh   text eol=lf

# ------------------------
# Configs
# ------------------------
*.ini  text eol=lf
*.cfg  text eol=lf
*.conf text eol=lf

# ------------------------
# Binary / Skip diff
# ------------------------
*.box  binary
*.crt  binary
*.key  binary
*.pem  binary
